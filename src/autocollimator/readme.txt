The files here are as follows: 

App.py - a persistent app, that users primarily interact with.  It provides a clean interface, handles configuration, and calls other modules / functions.
Workflow.py - This handles calling different functions, to *actually* get thigns done.  This can expand, while leaving the app relatively clean. 
Main.py - a useful entry point (not sure I fully understand this yet)

There other files here; though, I think they are largely self-explanatory. 
target_finding.py - find targets
sensor.py - handles the IO from the sensor.
test_report.py - generates test reports.

