# pytest.ini
[pytest]
minversion = 8.0
addopts = -ra --strict-markers --strict-config --cov=autocollimator --cov-report=term-missing
testpaths = tests
pythonpath = src
xfail_strict = true
markers =
    unit: fast unit tests
    integration: integration tests
